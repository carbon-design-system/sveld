import type { Component } from "svelte";

export type RunesRenderSkippedArgumentProps = {
  title: any;
};

export type RunesRenderSkippedArgumentExports = Record<string, never>;

declare const RunesRenderSkippedArgument: Component<
  RunesRenderSkippedArgumentProps,
  RunesRenderSkippedArgumentExports,
  ""
>;
export default RunesRenderSkippedArgument;
