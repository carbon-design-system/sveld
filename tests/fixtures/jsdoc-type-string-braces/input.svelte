<script>
  /**
   * @typedef {object} Config
   * @property {{ "a}": string }} weird Key with a closing brace.
   * @property {`x-${string}`} prefixed Template literal type.
   * @property {"{"} brace Open brace literal.
   * @property {'\'}'} escaped Escaped quote before a brace.
   */

  /** @type {Config} */
  export let config;
</script>
